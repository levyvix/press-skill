[project]
name = "press-skill"
version = "0.1.0"
description = "Desktop auto key presser with configurable interval and global hotkey"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "keyboard>=0.13.5",
    "pynput>=1.7.7",
]
